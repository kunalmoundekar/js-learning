// some more inssetion methode
//1 appned
//2 prepend  //inside at the beinning of the pararent
//3 before
//4 after
//5 replaceWith



// let newDiv = document.createElement("div");
// newDiv.innerHTML = "Hello, I am a new <b>DIV</b> element!";

// document.querySelector('.container').append(newDiv)
// document.querySelector('.container').prepend(newDiv)
// document.querySelector('.container').before(newDiv)
// document.querySelector('.container').after(newDiv)
// document.querySelector('.container').replaceWith(newDiv)


// insertAdjacentHTML

// "beforebegin"
// Before the element. Only valid if the element is in the DOM tree and has a parent element.

// "afterbegin"
// Just inside the element, before its first child.

// "beforeend"
// Just inside the element, after its last child.

// "afterend"
// After the element. Only valid if the element is in the DOM tree and has a parent element.

// let a = document.querySelector('.container')

// insertAdjacentText
// a.insertAdjacentText("beforebegin","mkjlkjlhiii kunal")
// a.insertAdjacentText("beforebegin","mkjlkjlhiii kunal")
// a.insertAdjacentText("beforeend" ,"mkjlkjlhiii kunal"   )
// a.insertAdjacentText("afterbegin" ,"mkjlkjlhiii kunal")
// a.insertAdjacentText("afterend" ,"mkjlkjlhiii kunal")

// let a = document.querySelector('.container')
// let newElement = document.createElement('div')
// newElement.innerText='mlmlml'
// a.insertAdjacentElement("beforebegin",newElement)










// insetrt acsseting element

